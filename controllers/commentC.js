const postComment = (req, res, next) => { 
  /**
   * _id
   * userId > req.user
   * username > req.username
   * movieId > req.param.movieId
   * text > req.body
   */
};

module.exports = {
  postComment
}